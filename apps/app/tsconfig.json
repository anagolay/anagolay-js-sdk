{
  "extends": "./.svelte-kit/tsconfig.json",
  "compilerOptions": {
    "types": ["node"],
    "typeRoots": ["src/@types", "node_modules/@types"],
    "paths": {
      "$src": ["src/"],
      "$src/*": ["src/*"],
      "$lib": ["src/lib"],
      "$lib/*": ["src/lib/*"]
    }
  }
}
