import split, { config } from '.'

describe('AnOperation: split.index', (): void => {
  it('is split defined', (): void => {
    expect(split).toBeDefined()
  })
  it('is config defined', (): void => {
    expect(config).toBeDefined()
  })
})
