import snFile from '.'

describe('Operation: snFile', (): void => {
  it('is defined', (): void => {
    expect(snFile).toBeDefined()
  })
  it('is should return proper file', (): void => {
    // @TODO
  })
})
