import matchAll, { config } from '.'

describe('AnOperation: matchAll', (): void => {
  it('is matchAll defined', (): void => {
    expect(matchAll).toBeDefined()
  })
  it('is config defined', (): void => {
    expect(config).toBeDefined()
  })
})
