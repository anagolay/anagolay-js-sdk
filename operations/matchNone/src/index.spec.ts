import matchNone, { config } from '.'

describe('AnOperation: matchNone', (): void => {
  it('is matchNone defined', (): void => {
    expect(matchNone).toBeDefined()
  })
  it('is config defined', (): void => {
    expect(config).toBeDefined()
  })
})
