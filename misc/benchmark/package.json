{
  "name": "benchmark",
  "version": "0.1.0",
  "files": ["lib"],
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "beautify-benchmark": "^0.2.4",
    "benchmark": "^2.1.4",
    "benchmarkjs-helper": "^1.0.1",
    "cbor": "^5.0.2",
    "fast-json-stringify": "^2.2.2",
    "microtime": "^3.0.0",
    "msgpack": "^1.0.3"
  },
  "scripts": {
    "clean": "rm -rf ./lib",
    "bench": "ts-node-dev -r tsconfig-paths/register  --cls --respawn --files --debug --transpileOnly --watch src/**/* -- src/index.ts"
  },
  "devDependencies": {
    "@types/benchmark": "^1.0.33"
  }
}
