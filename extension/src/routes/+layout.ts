// having this set to TRUE breaks the chrome imports
export const ssr = true;

// prerender our pages
export const prerender = true;

// setting this to true makes the SPA
export const csr = true;

// add .html to all our prerender pages
export const trailingSlash = 'never';
