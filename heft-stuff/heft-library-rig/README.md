# @kelp_digital/heft-library-rig

Profiles:

`default`:

- `api-extractor.json`
- `heft.json`
- `jest.config.json`
- `typescript.json` configured to emit the ESM and CJS 

`without-extractor`:

- `heft.json`
- `jest.config.json`
- `typescript.json` configured to emit the ESM and CJS 

