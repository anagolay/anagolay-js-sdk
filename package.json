{
  "author": "daniel@woss.io",
  "name": "root",
  "private": true,
  "workspaces": [
    "sdk/*",
    "operations/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/sensio_group/network-js-sdk.git"
  },
  "bugs": {
    "url": "https://gitlab.com/sensio_group/network-js-sdk/issues"
  },
  "homepage": "https://gitlab.com/sensio_group/network-js-sdk#readme",
  "license": "Apache-2.0",
  "scripts": {
    "boot": "lerna bootstrap",
    "lint": "eslint '*/**/*.{js,ts}' --quiet",
    "lint:fix": "eslint '*/**/*.{js,ts}' --quiet --fix",
    "build": "lerna run build",
    "test": "clear && jest --runInBand --no-cache",
    "test:watch": "yarn test --watch",
    "test:coverage": "jest --runInBand --coverage",
    "clean": "yarn lerna clean",
    "clean:all": "yarn clean && yarn clean:lib && yarn clean:root-node-modules",
    "clean:lib": "yarn clean:lib-dir && yarn clean:lib-tsinfo",
    "clean:lib-dir": "find . -name 'lib' -type d -prune -print -exec rm -rf '{}' \\;",
    "clean:lib-tsinfo": "find . -name 'tsconfig.tsbuildinfo' -type f -prune -print -exec rm -rf '{}' \\;",
    "clean:root-node-modules": "find . -name 'node_modules' -type d -prune -print -exec rm -rf '{}' \\;",
    "cli:regen-default-ops": "cd sdk/cli && yarn start regen-default-ops && cd ../../ && yarn std",
    "cli:regen-custom-ops": "cd sdk/cli && yarn start regen-custom-ops && cd ../../ && yarn std",
    "lerna:publish": "export GPG_TTY=$TTY && yarn lerna publish",
    "docs": "yarn typedoc --sourcefile-url-prefix \"https://gitlab.com/sensio_group/network-js-sdk/-/tree/master/\"",
    "core": "yarn workspace @sensio/core",
    "api": "yarn workspace @sensio/api",
    "cli": "yarn workspace @sensio/cli",
    "types": "yarn workspace @sensio/types",
    "postinstall": "yarn boot && ./remove-console-log-from-polkadot-logger.sh"
  },
  "engines": {
    "node": ">12"
  },
  "dependencies": {
    "@types/jest": "^26.0.14",
    "@types/node": "^14.11.2",
    "@typescript-eslint/eslint-plugin": "^4.2.0",
    "@typescript-eslint/parser": "^4.2.0",
    "babel-eslint": "^10.1.0",
    "concurrently": "^5.3.0",
    "eslint": "^7.9.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-import": "^2.22.0",
    "eslint-plugin-no-loops": "^0.3.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-prettier": "^3.1.4",
    "eslint-plugin-promise": "^4.2.1",
    "husky": "^4.3.0",
    "jest": "^26.4.2",
    "kind-of": "^6.0.3",
    "lerna": "^3.22.1",
    "lint-staged": "^10.4.0",
    "prettier": "^2.1.2",
    "ts-jest": "^26.4.0",
    "ts-node-dev": "^1.0.0-pre.49",
    "tsconfig-paths": "^3.9.0",
    "typedoc": "^0.19.2",
    "typedoc-plugin-lerna-packages": "^0.3.1",
    "typedoc-plugin-sourcefile-url": "^1.0.6",
    "typescript": "^4.0.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "yarn test"
    }
  },
  "lint-staged": {
    "*.{js,ts}": [
      "eslint --fix"
    ]
  },
  "resolutions": {
    "typedoc": "^0.19.2"
  }
}
